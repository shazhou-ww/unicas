{
  "name": "@unicas/docs-site",
  "version": "0.1.0",
  "private": true,
  "description": "Independently built and deployed UniCAS documentation site",
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "test": "vitest run --root . tests/docs-site.test.mjs",
    "test:browser": "pnpm run build && playwright test --config playwright.config.mjs",
    "typecheck": "tsc -b",
    "clean": "rimraf --glob dist .playwright test-results \"*.tsbuildinfo\"",
    "deploy": "pnpm run build && wrangler deploy --config wrangler.jsonc",
    "deploy:plan": "pnpm run build && wrangler deploy --dry-run --config wrangler.jsonc"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@unicas/space-protocol": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1",
    "@scalar/api-reference": "1.69.2",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "marked": "^18.0.13",
    "rimraf": "^6.1.3",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.20.0"
  }
}